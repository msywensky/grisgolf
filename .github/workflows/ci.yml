name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  frontend-build:
    name: Frontend build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20.x"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - run: npm ci
      - run: npm run check
      - run: npm run build

  backend-build:
    name: Backend build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - run: pip install -r requirements.txt

      # Import the app to catch syntax/import errors. Dummy Supabase config
      # is enough since this doesn't make any network calls.
      - name: Import check
        env:
          SUPABASE_URL: https://ci-placeholder.supabase.co
          SUPABASE_SERVICE_ROLE_KEY: ci-placeholder-key
        run: python -c "from app.main import app"

      # Build the actual Cloud Run image to catch Dockerfile/dependency drift.
      - name: Docker build
        run: docker build -t hmb-api-ci .

  deploy:
    name: Deploy to Cloud Run
    needs: [frontend-build, backend-build]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # Skip the actual deploy steps below when this push to main didn't
      # touch backend/ — no point rebuilding and redeploying the image for
      # a frontend-only change. Treat "can't diff" (force-push, new branch,
      # shallow history) as changed so we never silently skip a real deploy.
      - name: Check for backend changes
        id: changed
        run: |
          BEFORE="${{ github.event.before }}"
          if git cat-file -e "$BEFORE" 2>/dev/null && \
             git diff --quiet "$BEFORE" "${{ github.sha }}" -- backend/; then
            echo "backend=false" >> "$GITHUB_OUTPUT"
          else
            echo "backend=true" >> "$GITHUB_OUTPUT"
          fi

      - id: auth
        if: steps.changed.outputs.backend == 'true'
        uses: google-github-actions/auth@v2
        with:
          project_id: scramble-hmbgolf
          workload_identity_provider: ${{ vars.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ vars.GCP_DEPLOY_SA }}

      - if: steps.changed.outputs.backend == 'true'
        uses: google-github-actions/setup-gcloud@v2

      # Env vars and secrets already live on the Cloud Run service from the
      # initial manual deploy (see README) — this only ships the new image.
      - name: Deploy to Cloud Run
        if: steps.changed.outputs.backend == 'true'
        run: |
          gcloud run deploy hmb-scramble-api \
            --source backend \
            --region us-central1 \
            --quiet
